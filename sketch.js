let canvasW=400,canvasH=600;
let playerX,playerY,playerSpeed=5,playerRadius=14;
let bullets=[];
let enemies=[];
let particles=[];
let stars=[];
let score=0;
let gameOver=false;
let localFrame=0;
let prevSpace=false;
function setup(){createCanvas(canvasW,canvasH);playerX=canvasW/2;playerY=canvasH-40;for(let i=0;i<30;i++){let s={x:random(0,canvasW),y:random(0,canvasH),spd:random(1,2),size:random(1,3)};stars.push(s);}}
function draw(){localFrame++;background(0);fill(255);noStroke();for(let i=0;i<stars.length;i++){let s=stars[i];ellipse(s.x,s.y,s.size,s.size);s.y+=s.spd;if(s.y>canvasH){s.y=0;s.x=random(0,canvasW);s.spd=random(1,2);s.size=random(1,3);}}if(!gameOver){if(keyIsDown(LEFT_ARROW))playerX-=playerSpeed;if(keyIsDown(RIGHT_ARROW))playerX+=playerSpeed;playerX=constrain(playerX,playerRadius,canvasW-playerRadius);let spacePressed=keyIsDown(32);if(spacePressed&&!prevSpace){bullets.push({x:playerX,y:playerY-playerRadius,r:4,vy:-8});}prevSpace=spacePressed;if(localFrame%60===0){let ex=random(12,canvasW-12);enemies.push({x:ex,y:-12,r:12,vy:2});}for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y+=b.vy;if(b.y<-b.r)bullets.splice(i,1);}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];e.y+=e.vy;if(e.y-e.r>canvasH)enemies.splice(i,1);}for(let ei=enemies.length-1;ei>=0;ei--){let e=enemies[ei];for(let bi=bullets.length-1;bi>=0;bi--){let b=bullets[bi];let dx=b.x-e.x;let dy=b.y-e.y;let dist2=dx*dx+dy*dy;let radSum=b.r+e.r;if(dist2<=radSum*radSum){bullets.splice(bi,1);enemies.splice(ei,1);for(let p=0;p<5;p++){let ang=random(0,PI*2);let sp=random(0.5,2.5);particles.push({x:e.x,y:e.y,r:3,life:20,vx:cos(ang)*sp,vy:sin(ang)*sp});}score+=1;break;}}}for(let i=particles.length-1;i>=0;i--){let p=particles[i];p.x+=p.vx;p.y+=p.vy;p.life-=1;if(p.life<=0)particles.splice(i,1);}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];let dx=e.x-playerX;let dy=e.y-playerY;let d2=dx*dx+dy*dy;let pr=playerRadius+e.r;if(d2<=pr*pr){gameOver=true;break;}}}fill(0,0,255);ellipse(playerX,playerY,playerRadius*2,playerRadius*2);fill(255,255,0);for(let i=0;i<bullets.length;i++){let b=bullets[i];ellipse(b.x,b.y,b.r*2,b.r*2);}fill(255,0,0);for(let i=0;i<enemies.length;i++){let e=enemies[i];ellipse(e.x,e.y,e.r*2,e.r*2);}for(let i=0;i<particles.length;i++){let p=particles[i];let alpha=map(p.life,0,20,0,255);fill(255,150,0,alpha);ellipse(p.x,p.y,p.r*2,p.r*2);}fill(255);textSize(16);textAlign(LEFT,TOP);text('Score: '+score,8,8);if(gameOver){textSize(32);textAlign(CENTER,CENTER);fill(255,0,0);text('GAME OVER',canvasW/2,canvasH/2);} }
